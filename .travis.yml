language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t csmajer/docker-react  -f Dockerfile.dev .

  script:
    - docker run -e CI=true csmajer/docker-react npm run test -- --coverage